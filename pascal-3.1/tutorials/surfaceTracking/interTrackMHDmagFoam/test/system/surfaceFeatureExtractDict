/*--------------------------------*- C++ -*----------------------------------*\
| ========= | |
| \\ / F ield | OpenFOAM: The Open Source CFD Toolbox |
| \\ / O peration | Version: 2.2.2 |
| \\ / A nd | Web: www.OpenFOAM.org |
| \\/ M anipulation | |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version    2.0;
    format     ascii;
    class      dictionary;
    object     surfaceFeatureExtractDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

region_fluid.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_fluid_trackedSurface.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_fluid_fixedWall.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_fluid_movingWall.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_coil0.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_coil1.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_coil2.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_coil0_port.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_coil1_port.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

region_coil2_port.stl
{
    // How to obtain raw features (extractFromFile || extractFromSurface)
    extractionMethod extractFromSurface;

    extractFromSurfaceCoeffs
    {
        // Mark edges whose adjacent surface normals are at an angle less
        // than includedAngle as features
        // - 0 : selects no edges
        // - 180: selects all edges
        includedAngle 90;
    }

    // Write options
    // Write features to obj format for postprocessing
    writeObj yes;
}

// ************************************************************************* //
